# Enable the local dev-only content dashboard at /dashboard.
# WARNING: never set to "true" in production. The dashboard reads/writes
# localStorage drafts and is additionally gated by import.meta.env.DEV, so it
# cannot appear in a production build regardless of this flag.
VITE_ENABLE_DEV_DASHBOARD=false
